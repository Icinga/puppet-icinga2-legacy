<%= ERB.new(File.read(File.expand_path('_header.erb',File.dirname(File.dirname(file))))).result(binding) -%>

object FileLogger "mainlog" {
  severity = "<%= scope.lookupvar('severity') %>"
  path = "<%= scope.lookupvar('path') %>"
}
